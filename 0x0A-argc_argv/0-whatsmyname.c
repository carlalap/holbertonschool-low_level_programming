#include <stdlib.h>
#include <string.h>
#include <stdio.h>

/**
 * main - program that prints name of self
 * @argc: int num of inbound args
 * @argv: pointer to array of pointers w/ said args
 * Return: Always 0 if it pass
 */


int main(int argc, char *argv[])

{
	int i = 0;

	if (argc)
	{}

	printf("%s\n", argv[i]);
	return (0);
}
